Shell script to backup the home directory once every three minutes into a backup folder.
Backup is named according to the time it was created.

To run the script: 
1) chmod +x backup.sh
2) ./backup.sh

(change the backupFrom path accordingly)
